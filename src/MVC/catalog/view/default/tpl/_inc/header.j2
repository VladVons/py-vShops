<!-- _inc/header + -->
<script>
    gData.updValue('lang', {
        "added_to_cart": "{{lang.added_to_card}}",
        "already_in_cart": "{{lang.already_in_cart}}"
    })
</script>

<nav class="navbar navbar-expand-lg fixed-top navbar-dark vFooter">
    <div class="container">
        <div class="d-flex">
           <a href="/">
                <picture>
                    <source media="(min-width: 465px)" srcset="/default/assets/img/logo-1x1.png">
                    <img src="/default/assets/img/logo-x.png" alt="Your Logo" height="45" id="viLogo">
                </picture>
            </a>
            {% include './header_categories.j2' %}

            <form role="search" action="/submit.html" method="get">
                <div class="input-group vSearch">
                    <input type="hidden" name="route" value="product0/search"/>
                    <input type="text" class="form-control" name="q" placeholder="{{lang.search}}" list="viSearchSuggest" id="viSearchInput">
                            <datalist id="viSearchSuggest">
                            <option value="ukraine">
                            <option value="JS doesnt work properly">
                        </datalist>
                    <button class="btn btn-block" type="submit"><i class="fa fa-search" title="search"></i></button>
                </div>
            </form>
        </div>

        <div class="d-flex">
            <button type="button" class="btn btn-success" data-bs-toggle="modal" data-bs-target="#cartModal" id="viCartModal">
                <i class="fa fa-shopping-cart" title="{{lang.shopping_cart}}"></i>
                <span class="badge vBgBtn viTotalCount">3</span>
            </button>

            <ul class="navbar-nav">
                <li class="nav-item dropdown ms-2">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#"><i class="fa fa-dollar-sign" title="{{lang.currency}}"></i></a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="#">uah</a></li>
                        <li><a class="dropdown-item" href="#">usd</a></li>
                        <li><a class="dropdown-item" href="#">eur</a></li>
                    </ul>
                </li>
            </ul>
        </div>

  </div>
</nav>
<!-- _inc/header - -->
